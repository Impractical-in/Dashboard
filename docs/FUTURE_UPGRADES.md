# Future Upgrades

- Add backup file version history rotation on disk.
- Add backup integrity check and one-click repair from latest valid snapshot.
- Add password-protected encrypted backups (AES-GCM + PBKDF2/Argon2 key derivation) for export/import.
- Add optional auth + HTTPS hardening checklist before any public exposure.
